import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  main:{
    flex: 1 ,
    margin: 20,
    marginTop: 40,
    borderWidth: 3,
    borderColor: 'white',
    borderRadius: 30,
  },

  texto:{
    margin: 'auto',
    textAlign: 'center',
    fontSize: 20,
    color:'white',
  },

  titulo:{

    margin: 'auto',
    textAlign: 'center',
    fontSize: 40,
    color:'white',
  },
});


export {styles}