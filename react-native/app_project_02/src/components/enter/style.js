import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  texto:{
    textShadowColor: '#000',
    textShadowOffset: { width: -4, height: -4 },
    textShadowRadius: 1,
    textShadowColor: '#000',
    textShadowOffset: { width: 4, height: 4 },

    fontSize: 24,
    fontWeight: 'bold',
    margin: 'auto',
    textAlign: 'center',

    color:'white',
  },
});


export {styles}