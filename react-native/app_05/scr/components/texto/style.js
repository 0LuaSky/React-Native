import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    insert:{
    color: 'white',
    backgroundColor: 'gray',
    textAlign: 'center',
    borderColor: 'white',
    borderWidth: 1,
    height: 50,
    marginBottom: 11,
    borderRadius: 15,
    fontSize: 20,
  }

});

export {styles}