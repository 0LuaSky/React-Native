import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  text:{
    margin: 'auto',
    textAlign: 'center',
    fontSize: 20,
    color:'white',
    borderWidth: 1,
  },
});


export {styles}