import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    insert:{
    color: 'white',
    textAlign: 'center',
    borderColor: 'white',
    backgroundColor: 'gray',
    borderWidth: 1,
    height: 50,
    marginBottom: 11,
    borderRadius: 15,
    fontSize: 20,
  }

});

export {styles}